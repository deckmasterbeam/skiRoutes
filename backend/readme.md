# Ski Routes Backennd

Backend dev note:

- Do not assume `python` on PATH is the correct interpreter.
- Run backend commands with the specific Python installation/environment path where Flask and backend dependencies are installed.
- If a command fails under generic `python`, switch to the configured project interpreter for this workspace.

Start command on this machine:

- `C:/Users/joshu/AppData/Local/Python/pythoncore-3.14-64/python.exe c:/dev/skiRoutes/backend/app.py`